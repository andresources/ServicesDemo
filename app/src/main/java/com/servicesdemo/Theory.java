package com.servicesdemo;
public class Theory {
    /*
    1. service is started by using startService() method -> callback methods(onCreate(),  onStart(),onStop(),    onDestory())
    2. service is created by using bindService() method -> callback methods(onCreate(),   onBind(),onUnBind(),   onDestory())

    Service & Activity communiction:
    1.Create a Bound Service (Best for Two-way Communication)
2.Handlers
3.Broadcast receiver
4.Using LiveData
     */
}
